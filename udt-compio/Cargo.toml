[package]
name = "udt-compio"
version.workspace = true
edition.workspace = true

[dependencies]
udt-proto = { path = "../udt-proto" }
bytes = "1"
compio = { version = "0.14", features = ["runtime"] }
flume = "0.11"
