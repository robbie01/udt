[package]
name = "udt-integration-tests"
version.workspace = true
edition.workspace = true

[dev-dependencies]
udt-async = { path = "../../udt-async" }
tokio = { version = "1", features = ["full"] }
bytes = "1"
futures = { version = "0.3", default-features = false, features = ["executor"] }
